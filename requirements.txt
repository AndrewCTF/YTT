# Core runtime deps (captions path + CLI + library).
# Preferred install:  pip install yttranscript-mcp
requests>=2.31.0
aiosqlite>=0.20.0
click>=8.1.0
rich>=13.0.0

# Optional Whisper fallback (also needs ffmpeg):
#   pip install "yttranscript-mcp[whisper]"
# yt-dlp>=2024.0.0
# faster-whisper>=1.0.0
# torch>=2.0.0  # GPU
