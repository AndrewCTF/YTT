name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    name: python
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12"]

    steps:
      - uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          python-version: ${{ matrix.python-version }}

      # Tests are offline unit tests; only core + dev (+ mcp for import) needed.
      - name: Install the project
        run: uv sync --extra dev --extra mcp

      - name: Run tests
        run: uv run pytest

      - name: Lint
        run: uv run ruff check ytt/

      - name: Format check
        run: uv run black --check ytt/
